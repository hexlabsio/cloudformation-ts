import * as info from './info.json';
import {generate} from "./kloudformation/builder/generator";

describe('GO', () => {
  it('should', async () => {
   this.bucket
  });
});

this.bucket({
  bucketName: 'jimmy'
})
