import { Value } from '../../../kloudformation/Value';

export function zoneAwarenessConfigProps(zoneAwarenessConfigPropsProps: ZoneAwarenessConfigProps): ZoneAwarenessConfigProps { return (zoneAwarenessConfigPropsProps) }

export interface ZoneAwarenessConfigProps {
    availabilityZoneCount?: Value<number>;
}