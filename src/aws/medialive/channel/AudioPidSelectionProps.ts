import { Value } from '../../../kloudformation/Value';

export function audioPidSelectionProps(audioPidSelectionPropsProps: AudioPidSelectionProps): AudioPidSelectionProps { return (audioPidSelectionPropsProps) }

export interface AudioPidSelectionProps {
    pid?: Value<number>;
}