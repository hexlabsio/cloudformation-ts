import { Value } from '../../../kloudformation/Value';

export function targetGroupProps(targetGroupPropsProps: TargetGroupProps): TargetGroupProps { return (targetGroupPropsProps) }

export interface TargetGroupProps {
    arn: Value<string>;
}